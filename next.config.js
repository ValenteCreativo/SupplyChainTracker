/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['coral-near-warbler-359.mypinata.cloud', 'base-sepolia.easscan.org'],
    },
  }
  
  module.exports = nextConfig;
  